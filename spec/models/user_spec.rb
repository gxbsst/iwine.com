# -*- coding: utf-8 -*-
require "spec_helper"

describe User do

 # let(:user) { Factory(:user) }
 # let(:user) {User.first}
  it "Push one wine Timeline Event, The Feed number of User Will Change" do
    # event = Factory(:timeline_event)   
    # feed_count = user.feeds.count
    # user.push_one_event_item(user, event)
    # user.push_one_event_item(user, event)
    # user.push_one_event_item(user, event)
    # new_count = user.feeds.count
    # new_count.should == (feed_count + 3)
    pending
  end

  it "init_events_from_followings" do
   # @user = User.find(1)
   # user.init_events_from_followings
  end
  # it "should bending" do
  #   # pending
  # end

  # it "保存不成功" do
  #   @wine.region_tree_id = 11
  #   @wine.winery_id = 12
  #   @wine.save.should be_true
  # end

  
end


