<div class="bg_shadow">
  <div class="bg_bigshadow">
	<div id="maincontent" class="<%= set_layout_class_name %>">
		<% flash.each do |name, msg| %>
		<%= content_tag(:div, raw(msg), :id => "flash_#{name}") %>
		<% end %>
		<%= yield %>
    </div>
  </div>
  <div class="bg_shadow_bottom"></div>
</div>