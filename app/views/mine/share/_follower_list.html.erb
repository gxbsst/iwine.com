<div class="middle">
	<% @followers.map_user.each_with_index do |user, index|%>
	<% if index > 0 %>
	 <%= link_to_user user, user_path(user.id, :trailing_slash => true), :with_avatar => true, :avata_version => :thumb, :class => "w_50" %>
	 <% else %>
	 <%= link_to_user user, user_path(user.id, :trailing_slash => true), :with_avatar => true, :avata_version => :thumb, :class => "w_100" %>
	 <% end %>
	<% end %>	
</div>