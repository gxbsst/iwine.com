$("#trait_box").html("<%= escape_javascript(render('trait_template', :categories => @categories, :traits => @trait)) %>");

$.fancybox({
  padding: 0,
  maxWidth       : 870,
  maxHeight      : 715,
  fitToView       : false,
  width           : '100%',
  height          : 715,
  autoSize        : false,
  closeClick      : false,
  openEffect      : 'none',
  closeEffect     : 'none',
  href                  : '#trait_box',
  fixed: false,
//  centerOnScroll: false,
//  centerOnScroll: true,   for v 1.3.1
  helpers:         {
    overlay                      : {
      opacity                 : 0.8,
      css                     : {
        'background-color'  : '#FFF'
      }
    }
  } // end helper
});

//$.fancybox.center();
