$("#follow_form").html("<%= escape_javascript(render('follow_form', :follow => @follow, :followable => @followable)) %>");
$.fancybox({
        padding: 0,
         maxWidth       : 500,
        maxHeight       : 120,
        minHeight: 120,
        fitToView       : false,
        width           : '100%',
        height          : '100%',
        autoSize        : false,
        closeClick      : false,
        openEffect      : 'none',
        closeEffect     : 'none',
        'href'                  : '#follow_form',
         helpers        :         {
        overlay                      : {
            opacity                 : 0.8,
            css                     : {
                'background-color'  : '#FFF'
            }
        }
    } // end helper
});
